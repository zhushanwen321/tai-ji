---
'@zhushanwen/pi-system-prompt': minor
---

Appends a "TaiJi capabilities" section to the system prompt describing how markdown responses are rendered (HTML allowlist, relative image/link resolution against the session cwd, remote images not rendered). Configurable via the `capability.enabled` setting (defaults to on).
